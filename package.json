{
  "name": "maxilus-clinical",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de gestión para clínicas dentales",
  "scripts": {
    "dev": "npm run dev --workspace=client",
    "build": "npm run build --workspaces",
    "emulators": "npm run build --workspace=functions && firebase emulators:start"
  },
  "workspaces": [
    "client",
    "functions"
  ]
}
