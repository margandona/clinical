# Variables de entorno para desarrollo local de las Cloud Functions.
# Copiar a functions/.env (ignorado por git).

# Ruta al service account de Firebase (ver README para el archivo real).
# Solo se usa si NO estas corriendo via `firebase emulators:start`
# (el emulador ya provee credenciales locales automaticamente).
GOOGLE_APPLICATION_CREDENTIALS=../maxilus-dental-firebase-adminsdk-fbsvc-e0d15802e9.json
